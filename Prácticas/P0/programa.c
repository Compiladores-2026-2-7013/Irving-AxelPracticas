#include <stdio.h>
#include <stdlib.h>

//# ifdef PI
//# define area (r) (PI*r*r)
//# else
//# define area (r) (3.1416 * r * r)
//# endif

int main (void) {
        printf("Hola Mundo! \n"); //Imprimimos hola mundo
        float mi_area = 30;
	float numero= 20*20;
        printf("El resultado : %f\n ",mi_area);
        return 0;



}
